# UI Audit — Resilio+ Frontend — 2026-04-12

---

## Section A — Inventaire des écrans

### A.1 — Web App (`apps/web/src/app/`)

| Route | File path | Lines | Last git commit | Custom components imported | Status |
|---|---|---|---|---|---|
| `/` | `apps/web/src/app/page.tsx` | 16 | 2026-04-12 12:28 | none | **actif** — redirect router (→ `/dashboard` or `/onboarding`) |
| `/login` | `apps/web/src/app/login/page.tsx` | 92 | 2026-04-12 12:28 | `Button`, `Input`, `Label`, `Card/*` | **actif** — linked from `/onboarding` and auth redirects |
| `/onboarding` | `apps/web/src/app/onboarding/page.tsx` | 252 | 2026-04-12 12:28 | `Button`, `Input`, `Label`, `Card/*` | **actif** — root page redirects here when no token |
| `/dashboard` | `apps/web/src/app/dashboard/page.tsx` | 216 | 2026-04-12 12:28 | `ProtectedRoute`, `Card/*`, `Progress`, `Badge`, `Button` | **actif** — linked from TopNav |
| `/energy` | `apps/web/src/app/energy/page.tsx` | 441 | 2026-04-12 13:38 | `AllostaticGauge`, `recharts` (LineChart etc.) | **actif** — linked from TopNav |
| `/energy/cycle` | `apps/web/src/app/energy/cycle/page.tsx` | 259 | 2026-04-12 12:28 | none (static, no `@/components` or `@resilio/` imports) | **orphelin** — no incoming links found in any page or nav |
| `/check-in` | `apps/web/src/app/check-in/page.tsx` | 491 | 2026-04-12 13:38 | none (`@/lib/auth`, `@/lib/api` only) | **actif** — linked from TopNav + dashboard |
| `/plan` | `apps/web/src/app/plan/page.tsx` | 100 | 2026-04-12 12:28 | `ProtectedRoute`, `Card/*`, `Button` | **actif** — linked from TopNav + dashboard |
| `/review` | `apps/web/src/app/review/page.tsx` | 142 | 2026-04-12 12:28 | `ProtectedRoute`, `Card/*`, `Button`, `Input`, `Label` | **actif** — linked from TopNav + dashboard |
| `/history` | `apps/web/src/app/history/page.tsx` | 80 | 2026-04-12 12:28 | `ProtectedRoute`, `Card/*`, `Badge`, `Progress` | **actif** — linked from TopNav |
| `/analytics` | `apps/web/src/app/analytics/page.tsx` | 115 | 2026-04-12 12:28 | `AcwrTrendChart`, `TrainingLoadChart`, `SportBreakdownChart`, `PerformanceTrendChart` | **actif** — linked from TopNav |
| `/session/[id]` | `apps/web/src/app/session/[id]/page.tsx` | 140 | 2026-04-12 12:28 | `ProtectedRoute`, `Card/*`, `Badge`, `Button` | **actif** — linked from `/plan` via `session/{id}` |
| `/session/[id]/log` | `apps/web/src/app/session/[id]/log/page.tsx` | 243 | 2026-04-12 12:28 | `ProtectedRoute`, `Card/*`, `Button`, `Input`, `Label` | **actif** — linked from `/session/[id]` |
| `/tracking` | `apps/web/src/app/tracking/page.tsx` | 366 | 2026-04-12 12:28 | `ProtectedRoute`, `Card/*`, `Badge`, `Button`, `Input`, `Label` | **actif** — linked from TopNav (tracking_only mode) |
| `/tracking/import` | `apps/web/src/app/tracking/import/page.tsx` | 207 | 2026-04-12 12:28 | `ProtectedRoute`, `Card/*`, `Button`, `Badge` | **actif** — linked from `/tracking` |
| `/settings` | `apps/web/src/app/settings/page.tsx` | 11 | 2026-04-12 12:28 | none | **actif** — immediate redirect to `/settings/connectors` |
| `/settings/connectors` | `apps/web/src/app/settings/connectors/page.tsx` | 362 | 2026-04-12 12:28 | `ProtectedRoute`, `Card/*`, `Button`, `Input` | **actif** — linked from TopNav ("Settings") |

**Notes:**
- `/energy/cycle` is the only true orphan: no `href="/energy/cycle"` found anywhere in `apps/web/src/`. It contains static demo data (`DEMO_CYCLE_DAY = 18`) and uses hardcoded hex colors throughout. No link from `/energy` to `/energy/cycle`.
- `/analytics` has no `ProtectedRoute` wrapper but implements inline auth guard (`if (!token) router.push("/login")`).
- `/check-in` and `/energy` implement inline auth guard via `useAuth()` + `router.replace('/login')` rather than `ProtectedRoute`.
- `/settings` is a pure redirect shell (11 lines, `router.replace('/settings/connectors')`).

### A.2 — Mobile App (`apps/mobile/app/` — Expo Router)

| Route | File path | Lines | Last git commit | Imports from packages | Status |
|---|---|---|---|---|---|
| `/` (index) | `apps/mobile/app/index.tsx` | 5 | 2026-04-12 13:32 | `expo-router` (`Redirect`) | **actif** — redirects to `/login` |
| `/_layout` | `apps/mobile/app/_layout.tsx` | 15 | 2026-04-12 13:32 | `expo-router` (`Stack`), `@resilio/ui-mobile` (`ThemeProvider`) | **actif** — root layout |
| `/login` | `apps/mobile/app/login.tsx` | 107 | 2026-04-12 13:32 | `@resilio/ui-mobile` (`Button`, `Card`, `Input`), `@resilio/design-tokens` (`colors`) | **actif** — entry screen |
| `/dashboard` | `apps/mobile/app/dashboard.tsx` | 105 | 2026-04-12 13:32 | `@resilio/ui-mobile` (`Card`, `Icon`), `@resilio/design-tokens` (`colors`) | **actif** — main screen after login |
| `/check-in` | `apps/mobile/app/check-in.tsx` | 103 | 2026-04-12 13:32 | `@resilio/ui-mobile` (`Button`, `Icon`), `@resilio/design-tokens` (`colors`) | **actif** — linked from dashboard |

No TODO/FIXME/WIP markers found in any mobile screen.

---

## Section B — Inventaire des composants partagés

### B.1 — `apps/web/src/components/`

| Component | File path | Usage in repo (files importing) |
|---|---|---|
| `AcwrTrendChart` | `analytics/AcwrTrendChart.tsx` | 1 (`/analytics` page) |
| `PerformanceTrendChart` | `analytics/PerformanceTrendChart.tsx` | 1 (`/analytics` page) |
| `SportBreakdownChart` | `analytics/SportBreakdownChart.tsx` | 1 (`/analytics` page) |
| `TrainingLoadChart` | `analytics/TrainingLoadChart.tsx` | 1 (`/analytics` page) |
| `ProtectedRoute` | `protected-route.tsx` | 9 pages |
| `TopNav` | `top-nav.tsx` | 1 (`layout.tsx`) |
| `AllostaticGauge` | `ui/allostatic-gauge.tsx` | 1 (`/energy` page) |
| `Badge` | `ui/badge.tsx` | 7 pages |
| `Button` | `ui/button.tsx` | 12 pages |
| `Card` / `CardContent` / `CardHeader` / `CardTitle` / `CardDescription` | `ui/card.tsx` | 13 pages |
| `Input` | `ui/input.tsx` | 8 pages |
| `Label` | `ui/label.tsx` | 7 pages |
| `Progress` | `ui/progress.tsx` | 4 pages |

**Note:** `apps/web/src/components/ui/` contains shadcn/ui components (`badge`, `button`, `card`, `input`, `label`, `progress`). These are NOT re-exported from `@resilio/ui-web` — `ui-web` only exports `Icon`, `ThemeProvider`, and `useTheme`. The shadcn components live directly in the app.

### B.2 — `packages/ui-web/src/`

| Export | File | Usage in repo |
|---|---|---|
| `Icon` (lucide-react abstraction, 30+ semantic names) | `icons.ts` | `top-nav.tsx` (`Icon.LightMode`, `Icon.DarkMode`) |
| `ThemeProvider` | `theme/ThemeProvider.tsx` | `apps/web/src/app/layout.tsx` |
| `useTheme` | `theme/useTheme.ts` | Re-exported, used internally |

### B.3 — `packages/ui-mobile/src/`

| Component | File | Usage in `apps/mobile/` |
|---|---|---|
| `Button` | `components/Button.tsx` | 3 files |
| `Card` | `components/Card.tsx` | 2 files |
| `Input` | `components/Input.tsx` | 1 file |
| `ThemeProvider` | `theme/ThemeProvider.tsx` | `_layout.tsx` |
| `Icon` | `icons.ts` | `dashboard.tsx`, `check-in.tsx` |

### B.4 — Doublons identifiés

| Pattern | Location A | Location B | Verdict |
|---|---|---|---|
| `Card` component | `apps/web/src/components/ui/card.tsx` (shadcn) | `packages/ui-mobile/src/components/Card.tsx` (RN) | Not a true duplicate — different platforms (web DOM vs React Native). By design. |
| `Button` component | `apps/web/src/components/ui/button.tsx` (shadcn) | `packages/ui-mobile/src/components/Button.tsx` (RN) | Same — platform-split, intentional. |
| `Input` component | `apps/web/src/components/ui/input.tsx` (shadcn) | `packages/ui-mobile/src/components/Input.tsx` (RN) | Same — platform-split, intentional. |
| `ThemeProvider` | `packages/ui-web/src/theme/ThemeProvider.tsx` (next-themes wrapper) | `packages/ui-mobile/src/theme/ThemeProvider.tsx` (RN ColorScheme context) | Different implementations for different runtimes. Intentional. |
| shadcn components in `apps/web/` | `apps/web/src/components/ui/*` | `packages/ui-web/` | **Structural gap**: shadcn components are NOT in `@resilio/ui-web`, they live directly in the app. This means `apps/desktop` cannot reuse them via package import. |

---

## Section C — Design system actuel

### C.1 — CSS Variables déclarées (`apps/web/src/app/globals.css`)

All variables are declared inside `:root {}` (dark defaults) with overrides in `.light {}`.

#### `:root` — dark defaults

| Variable | Value |
|---|---|
| `--background` | `#08080e` |
| `--surface-1` | `#0f0f18` |
| `--surface-2` | `#14141f` |
| `--surface-3` | `#1a1a28` |
| `--border` | `#22223a` |
| `--border-subtle` | `#191928` |
| `--foreground` | `#eeeef4` |
| `--text-muted` | `#5c5c7a` |
| `--text-secondary` | `#8888a8` |
| `--accent` | `#5b5fef` |
| `--accent-dim` | `rgba(91, 95, 239, 0.15)` |
| `--zone-green` | `#10b981` |
| `--zone-green-bg` | `rgba(16, 185, 129, 0.10)` |
| `--zone-yellow` | `#f59e0b` |
| `--zone-yellow-bg` | `rgba(245, 158, 11, 0.10)` |
| `--zone-red` | `#ef4444` |
| `--zone-red-bg` | `rgba(239, 68, 68, 0.10)` |
| `--zone-critical` | `#dc2626` |
| `--accent-rgb` | `91, 95, 239` |
| `--zone-green-rgb` | `16, 185, 129` |
| `--zone-yellow-rgb` | `245, 158, 11` |
| `--zone-red-rgb` | `239, 68, 68` |
| `--zone-critical-rgb` | `220, 38, 38` |
| `--phase-luteal-rgb` | `129, 140, 248` |
| `--phase-menstrual` | `#ef4444` |
| `--phase-follicular` | `#10b981` |
| `--phase-ovulation` | `#f59e0b` |
| `--phase-luteal` | `#818cf8` |
| `--card` | `#14141f` |
| `--card-foreground` | `#eeeef4` |
| `--muted` | `#1a1a28` |
| `--muted-foreground` | `#5c5c7a` |
| `--primary` | `#5b5fef` |
| `--primary-foreground` | `#ffffff` |
| `--secondary` | `#1a1a28` |
| `--secondary-foreground` | `#8888a8` |
| `--destructive` | `#ef4444` |
| `--destructive-foreground` | `#ffffff` |
| `--input` | `#1a1a28` |
| `--ring` | `#5b5fef` |

### C.2 — Doublons dans `:root`

Three variables are declared **twice** within the same `:root {}` block (lines 9–66 of `globals.css`). The second declaration (shadcn semantic section, lines 51–65) overwrites the first with an identical value:

| Variable | First declaration (line) | Second declaration (line) | Values identical? |
|---|---|---|---|
| `--background` | line 11 (`#08080e`) | line 51 (`#08080e`) | Yes |
| `--foreground` | line 19 (`#eeeef4`) | line 52 (`#eeeef4`) | Yes |
| `--border` | line 15 (`#22223a`) | line 57 (`#22223a`) | Yes |

These are redundant but not harmful (values are identical). The CSS comment at line 50 "Shadcn semantic tokens" explains the intent: the first block is the Resilio spatial hierarchy, the second block maps to shadcn-expected names. Consolidation recommended.

### C.3 — Variables déclarées mais jamais utilisées dans le code

Variables declared in `:root` of `globals.css` but with zero occurrences of `var(--<name>)` found in `apps/web/src/**`:

| Variable | Declared value | Status |
|---|---|---|
| `--accent-dim` | `rgba(91, 95, 239, 0.15)` | Declared, never used via `var()` |
| `--surface-3` | `#1a1a28` | Declared, never used via `var()` |
| `--phase-menstrual` | `#ef4444` | Declared, never used via `var()` |
| `--phase-follicular` | `#10b981` | Declared, never used via `var()` |
| `--phase-ovulation` | `#f59e0b` | Declared, never used via `var()` |
| `--zone-critical-rgb` | `220, 38, 38` | Declared, never used via `var()` |

**Note:** These variables are declared but components that logically need them (e.g., `energy/cycle/page.tsx`, `allostatic-gauge.tsx`) use hardcoded hex values instead of `var()` — which is the root cause of the debt documented in Section E.

### C.4 — Variables utilisées mais non déclarées

All `var(--xxx)` calls found in `apps/web/src/` resolve to declared variables. No missing declarations detected.

### C.5 — `packages/design-tokens/` — tokens TypeScript

The design-tokens package (`@resilio/design-tokens`) exports JS/TS constants mirroring the CSS variables:

| Module | Key exports |
|---|---|
| `colors.ts` | `colors.dark.*`, `colors.light.*`, `colors.primary`, `colors.zone*`, `colors.phase*`, `colors.shadcn.*` |
| `typography.ts` | `typography.fontSans`, `typography.fontMono`, `typography.size.*`, `typography.weight.*`, `typography.lineHeight.*`, `typography.tracking.*` |
| `spacing.ts` | `spacing.1` through `spacing.24` (4px–96px scale) |
| `radius.ts` | `radius.sm` (4px) through `radius.full` (9999px) |
| `shadows.ts` | `shadows.dark.*` and `shadows.light.*` (4 elevation levels) |
| `animation.ts` | `animation.duration.*` and `animation.easing.*` |

**Usage:** `@resilio/design-tokens` is imported **only by `apps/mobile/`** and `packages/ui-mobile/`. The web app (`apps/web/`) does NOT import design-tokens — it uses CSS variables directly. This means the TypeScript token values and the CSS variable values are maintained separately, creating a sync risk.

---

## Section D — Fonts et assets

### D.1 — Chargement des fonts (web)

| Method | Location | Detail |
|---|---|---|
| `@import url()` (Google Fonts CDN) | `apps/web/src/app/globals.css` line 1 | `Space Grotesk` (wght 300;400;500;600;700) + `Space Mono` (wght 400;700) via `fonts.googleapis.com` |
| CSS `font-family` declaration | `globals.css` body rule | `'Space Grotesk', system-ui, sans-serif` |
| `@theme inline` mapping | `globals.css` lines 68–86 | `--font-sans` and `--font-mono` mapped to Tailwind theme |

`next/font` is **not used** — fonts are loaded via Google Fonts `@import`, which means no build-time font optimization (no font subsetting, no self-hosting, network dependency on Google CDN). This differs from the Next.js recommended approach.

`layout.tsx` does **not** use `next/font/google`. There is no `localFont()` call.

### D.2 — Assets `apps/web/public/`

| File | Purpose |
|---|---|
| `splash.png` | App splash screen (light) |
| `splash-dark.png` | App splash screen (dark) |
| `next.svg` | Next.js logo (default scaffold asset) |
| `vercel.svg` | Vercel logo (default scaffold asset) |
| `globe.svg` | Default scaffold SVG |
| `file.svg` | Default scaffold SVG |
| `window.svg` | Default scaffold SVG |
| `custom.css` | Placeholder file (`/* custom placeholder */` — 1 line) |
| `loader.js` | Placeholder file (`// loader placeholder` — 1 line) |

**Note:** `next.svg`, `vercel.svg`, `globe.svg`, `file.svg`, `window.svg` appear to be default Next.js scaffold assets not referenced by any application code. `custom.css` and `loader.js` are empty stubs with no current purpose.

### D.3 — Assets `apps/mobile/`

No `assets/` directory exists in `apps/mobile/`. The mobile app has no static assets.

---

## Section E — Dette technique

### E.1 — Hardcoded hex colors (violations de la règle 3 de CLAUDE.md)

Files containing hardcoded hex color values outside `packages/design-tokens/`:

| File | Hex occurrences | Representative values |
|---|---|---|
| `apps/web/src/app/energy/cycle/page.tsx` | 40 | `#5c5c7a`, `#8888a8`, `#5b5fef`, `#22223a`, `#fff`, `#14141f`, `#1a1a28`, `#f59e0b`, `#ef4444`, `#eeeef4`, `#818cf8`, `#10b981` |
| `apps/web/src/components/ui/allostatic-gauge.tsx` | 14 | `#10b981`, `#f59e0b`, `#ef4444`, `#dc2626`, `#22223a`, `#5c5c7a`, `#08080e`, zone bg with alpha (`#10b98133`, etc.) |
| `apps/web/src/components/analytics/SportBreakdownChart.tsx` | 5 | Sport-specific chart colors |
| `apps/web/src/app/dashboard/page.tsx` | 3 | Inline style values |
| `apps/web/src/components/analytics/AcwrTrendChart.tsx` | 3 | Chart stroke colors |
| `apps/web/src/components/analytics/TrainingLoadChart.tsx` | 3 | Chart stroke colors |
| `apps/web/src/components/analytics/PerformanceTrendChart.tsx` | 2 | Chart stroke colors |
| `apps/web/src/app/globals.css` | (intentional — source of truth) | n/a |

**Total non-CSS-var hex occurrences in components/pages:** ~70 across 7 files.

**Root cause:** `energy/cycle/page.tsx` and `allostatic-gauge.tsx` predate the V3 design system migration. The `session/fe-1c-eslint-hex-cleanup` branch addressed `check-in` and `energy` pages (merged as commit `5a6b5e1`) but did not address `energy/cycle`, `allostatic-gauge`, or the analytics chart components.

### E.2 — Direct `lucide-react` imports (violations de la règle 1)

**Zero violations found.** Grep of `from 'lucide-react'` across `apps/web/src/`, `apps/desktop/`, and `apps/mobile/` returns no results. The `Icon` abstraction layer in `packages/ui-web/src/icons.ts` is the sole importer.

### E.3 — Unused variable (`allostatic-gauge.tsx`)

In `apps/web/src/components/ui/allostatic-gauge.tsx` line 63:

```typescript
const normTo360 = (deg: number) => ((deg - 1) % 360) + 1
```

This function is declared but **never called** anywhere in the file. It will trigger `@typescript-eslint/no-unused-vars`. Occurrences of `normTo360`: 1 (declaration only).

### E.4 — `any` types

No `any` type annotations found in `energy/cycle/page.tsx` or `allostatic-gauge.tsx`.

### E.5 — Composants sans `ProtectedRoute` wrap

Pages that serve authenticated content but do not use `ProtectedRoute`:

| Page | Auth guard method |
|---|---|
| `/analytics` | Inline `if (!token) router.push('/login')` inside `useEffect` |
| `/check-in` | Inline `if (!athleteId) router.replace('/login')` inside async function |
| `/energy` | Inline `router.replace('/login')` inside 401 error handler |
| `/energy/cycle` | **No auth guard at all** — fully public static page |

Inconsistent auth guard pattern: 9 pages use `ProtectedRoute`, 3 use inline redirects, 1 (`/energy/cycle`) has no guard.

### E.6 — `fetch()` calls bypassing `@resilio/api-client`

One `fetch()` call exists directly in `apps/web/src/lib/api.ts` (line 309):

```typescript
const res = await fetch(`${API_BASE}/athletes/${athleteId}/external-plan/import`, {
```

This is **inside `api.ts`** (the api client module itself), not in a page or component. No pages or components call `fetch()` directly. No `axios` usage found anywhere. This is not a violation — it is an implementation detail of the api client.

### E.7 — `energy/cycle/page.tsx` — static demo data not connected to backend

The page uses hardcoded constants (`DEMO_CYCLE_DAY = 18`, `DEMO_CYCLE_LENGTH = 28`, `DEMO_PHASE = 'luteal'`) with no API calls, no `useAuth`, and no `ProtectedRoute`. It is a static demo/prototype with no real data integration.

---

## Section F — État Git

### F.1 — Branches locales

```
feat/backend-finalize
feat/connectors
feat/phase10
feat/phase11
* main
session/app-works
session/fe-1a-tauri-desktop
session/fe-1b-expo-mobile
session/fe-1c-eslint-hex-cleanup
session/fe-1d-api-client
session/fe-vague1-postmortem
session/frontend-s0-monorepo-setup
session/s1-external-plan
session/s1-external-plan-clean
session/s2-plan-import
session/s3-weekly-review
session/s4-energy-patterns
session/s5-frontend-energy
session/s6-frontend-tracking
session/s7-e2e-finalisation
session/s7b-finalisation-complete
v3
v3-athletestate
v3-energy-coach
v3-frontend
v3-hormonal-ea
v3-knowledge
v3-recovery-coach
```

### F.2 — Branches concernant `session/fe-1a` et `session/fe-1c`

- `session/fe-1a-tauri-desktop` — **existe** (local + `remotes/origin`)
- `session/fe-1c-eslint-hex-cleanup` — **existe** (local only — not pushed to origin)

Note: The naming in the audit request (`session/fe-1a`, `session/fe-1c`) matches the prefixes of `session/fe-1a-tauri-desktop` and `session/fe-1c-eslint-hex-cleanup`.

### F.3 — 10 derniers commits

```
552402b Merge branch 'session/app-works'
58fbbd4 docs(session): app-works session report and notes
251cccf Merge branch 'session/fe-vague1-postmortem'
eafa7da docs(CLAUDE): add Vague 1 completion + worktree rule
c871336 docs(master-v1): mark FE-1A/B/C/D complete with Vague 1 notes
2555c33 docs(vague1): add frontend Vague 1 postmortem
b8932b2 chore: update pnpm-lock after fe-1b merge
d4e1ac5 Merge branch 'session/fe-1b-expo-mobile'
ac2cba5 Merge branch 'session/fe-1d-api-client'
5a6b5e1 feat(web): add ESLint rules + migrate check-in and energy hex to CSS vars
```

### F.4 — Working tree

Clean. No uncommitted files. Branch `main` is up to date with `origin/main`.

---

## Section G — Recommandations d'archivage

### G.1 — À garder tel quel

Code conforme aux règles CLAUDE.md, aligné avec la V3 architecture.

| File / Module | Raison |
|---|---|
| `packages/design-tokens/` (all files) | Source of truth correctement structurée. Valeurs cohérentes avec `globals.css`. Utilisée par mobile. |
| `packages/ui-web/src/icons.ts` | Abstraction lucide-react correctement encapsulée. Zéro violation de la règle 1. |
| `packages/ui-web/src/theme/ThemeProvider.tsx` | Wrapper next-themes propre, utilisé dans layout. |
| `packages/ui-mobile/src/` (tous composants) | Importent de `@resilio/design-tokens`. Conforme règle 2 et 5. |
| `apps/web/src/app/globals.css` | Source CSS correcte malgré les 3 doublons mineurs. |
| `apps/web/src/components/protected-route.tsx` | Simple, correcte, largement utilisée. |
| `apps/web/src/components/top-nav.tsx` | Utilise `@resilio/ui-web` (`Icon`), pas de `lucide-react` direct. |
| `apps/web/src/components/ui/badge.tsx`, `button.tsx`, `card.tsx`, `input.tsx`, `label.tsx`, `progress.tsx` | Composants shadcn standard, bien utilisés. À terme migrer vers `@resilio/ui-web`. |
| `apps/web/src/app/dashboard/page.tsx` | Correctement protégé, utilise api client. (3 hex mineurs à corriger.) |
| `apps/web/src/app/login/page.tsx`, `onboarding/page.tsx` | Flux d'auth corrects, pas de hex hardcodé. |
| `apps/web/src/app/plan/page.tsx`, `review/page.tsx`, `history/page.tsx` | Protégés, utilisent api client, pas de hex hardcodé. |
| `apps/web/src/app/session/[id]/page.tsx`, `session/[id]/log/page.tsx` | Protégés, propres. |
| `apps/web/src/app/settings/connectors/page.tsx` | Protégé, propre. |
| `apps/web/src/app/tracking/page.tsx`, `tracking/import/page.tsx` | Protégés, propres. |
| `apps/mobile/app/_layout.tsx`, `index.tsx`, `login.tsx`, `dashboard.tsx`, `check-in.tsx` | Tous utilisent `@resilio/ui-mobile` et `@resilio/design-tokens`. Conformes. |

### G.2 — À refactorer

Code utile mais violant les règles architecturales ou le design system.

| File | Problème | Action recommandée |
|---|---|---|
| `apps/web/src/components/ui/allostatic-gauge.tsx` | 14 hex hardcodés, variable `normTo360` non utilisée | Remplacer hex par `var(--zone-green)` etc., supprimer `normTo360` |
| `apps/web/src/app/energy/cycle/page.tsx` | 40 hex hardcodés, aucune auth guard, données statiques (`DEMO_*`) | Ajouter `ProtectedRoute`, remplacer hex par CSS vars, connecter au backend (cycle tracking API) |
| `apps/web/src/components/analytics/AcwrTrendChart.tsx`, `TrainingLoadChart.tsx`, `SportBreakdownChart.tsx`, `PerformanceTrendChart.tsx` | Hex hardcodés dans `stroke`, `fill` props Recharts | Utiliser les CSS vars via `getComputedStyle` ou constantes de `@resilio/design-tokens` |
| `apps/web/src/app/analytics/page.tsx` | Auth guard inline non uniforme (devrait utiliser `ProtectedRoute`) | Remplacer by `<ProtectedRoute>` wrapper |
| `apps/web/src/app/check-in/page.tsx` | Auth guard inline non uniforme | Remplacer par `<ProtectedRoute>` wrapper |
| `apps/web/src/app/energy/page.tsx` | Auth guard inline (401 handler uniquement) | Ajouter `<ProtectedRoute>` |
| `apps/web/src/app/globals.css` | 3 variables déclarées 2 fois dans `:root` (`--background`, `--foreground`, `--border`) | Fusionner les deux blocs `:root` en un seul bloc ordonné |
| `apps/web/src/lib/api.ts` (migration en cours) | Certaines pages utilisent encore `@/lib/api` directement plutôt que `@resilio/api-client` | Migration progressive vers `@resilio/api-client` (règle 4, marquée "en cours" dans CLAUDE.md) |
| `apps/web/public/custom.css`, `loader.js` | Fichiers placeholder vides (1 ligne chacun) | Supprimer ou documenter l'intention |
| `apps/web/public/next.svg`, `vercel.svg`, `globe.svg`, `file.svg`, `window.svg` | Assets scaffold Next.js jamais référencés par l'application | Supprimer |
| Font loading via `@import url()` CDN | Pas de `next/font` = pas d'optimisation build-time | Migrer vers `next/font/google` dans `layout.tsx` |

### G.3 — À archiver (déplacer vers `docs/archive/frontend-v1/`)

Code devenu sans objet ou remplacé par une meilleure implémentation.

| File | Raison d'archivage |
|---|---|
| `apps/web/src/app/energy/cycle/page.tsx` | Page orpheline sans liens entrants, données statiques de démo, aucune intégration backend. Si le cycle hormonal n'est pas dans la roadmap immédiate, archiver jusqu'à implémentation réelle. |
| `apps/web/public/custom.css` | Fichier stub sans contenu. Aucune valeur. |
| `apps/web/public/loader.js` | Fichier stub sans contenu. Aucune valeur. |
| `apps/web/src/app/settings/page.tsx` | 11 lignes de redirect pur. Pourrait être remplacé par une règle de redirection Next.js dans `next.config.ts` (`redirects()`). |

---

*Audit produit le 2026-04-12. Toutes les informations sont extraites directement du code source — aucune valeur inventée.*
